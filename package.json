{
  "name": "zero-to-tech",
  "version": "1.0.0",
  "scripts": {
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  },
  "dependencies": {
    "animejs": "^4.5.0"
  }
}
